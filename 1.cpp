#include<bits/stdc++.h>
using namespace std;
typedef ll long long;

int subsetSum(int arr[], int n, int a, int b) {
    ll sum = 0;
    rec()
}

int main() {
    int n, a, b;
    cin >> n >> a >> b;
    int arr[n];
    for(int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    cout<<subsetSum(arr, n, a, b);
}